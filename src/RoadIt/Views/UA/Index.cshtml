@model RoadIt.Models.UA

@{
    ViewBag.Title = "UA report";
}

<h2>UA report</h2>
@{
    if (Session["RoleId"].ToString() == "6")
    {
        if (Session["RoadId"].ToString() != null)
        {
            <p>The next data that is showed is for roadsection: @Session["RoadID"].ToString()</p>
            @Html.Raw(Session["Planning"].ToString())
            @Html.Raw(Session["AsphaltMixPlant"].ToString())
            @Html.Raw(Session["Silo"].ToString())
            @Html.Raw(Session["AggagaratesBitumenComposition"].ToString())
            @Html.Raw(Session["Truck"].ToString())
            @Html.Raw(Session["TruckTime"].ToString())
            @Html.Raw(Session["TruckStop"].ToString())
            @Html.Raw(Session["Position"].ToString())
            @Html.Raw(Session["PositionReturn"].ToString())
            @Html.Raw(Session["Finisher"].ToString())
            @Html.Raw(Session["FinisherSpeed"].ToString())
            @Html.Raw(Session["wheater"].ToString())
            @Html.Raw(Session["FinisherStop"].ToString())
            @Html.Raw(Session["Compactor"].ToString())
            @Html.Raw(Session["QualityControl"].ToString())
        }
        else
        {
            ViewBag.error = "Choose first a roadsection";
            @ViewBag.error
        }
    }
    else
    {
        ViewBag.error = "You are not authorized to view this page";
        @ViewBag.error
    }   
   
} 